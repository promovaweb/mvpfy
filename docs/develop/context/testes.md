# Contexto de testes

Os testes verificam catálogo, metadados de skills, criação de projeto,
persistência, migração e validação do `MVP.md`. A documentação também deve
ser conferida com Markdown lint e o ebook deve passar pelo manifesto e pelos
checks de PDF e EPUB.
